#ifndef _ANSWER_EXTRACTOR_H_
#define _ANSWER_EXTRACTOR_H_

#include <vector>
#include <string>
#include <utility>
#include <sstream>
#include "Generic/common/bsp_declare.h"
#include "ACEDecoder.h"
#include "ACEPREMDecoder.h"
/*
class ACEEvent;

class BestBuyerThemeSellerByThreshold : public PREMAdapter {
	public:
		BestBuyerThemeSellerByThreshold(const ACEPREMDecoder_ptr& decoder, 
				double threshold) 
			: PREMAdapter(decoder), _threshold(threshold) {}

		EntityLabelling decode(const GoldStandardACEInstance& event) const;
		std::string name() const { 
			return "BestBuyerThemeSellerByThreshold";
		}
	private:
		double _threshold;
};

class MaxVarThreshold : public PREMAdapter {
public:
	MaxVarThreshold(const ACEPREMDecoder_ptr& decoder, double threshold) 
		: PREMAdapter(decoder), _threshold(threshold) {}
	EntityLabelling decode(const GoldStandardACEInstance& event) const;
	std::string name() const { 
		return "MaxVarThreshold";
	}
private:
	double _threshold;
};

class MaxSentenceThreshold : public PREMAdapter {
public:
	MaxSentenceThreshold(const ACEPREMDecoder_ptr& decoder, double threshold) 
			: PREMAdapter(decoder), _threshold(threshold) {}
	EntityLabelling decode(const GoldStandardACEInstance& event) const;
	std::string name() const { 
		return "MaxSentenceThreshold";
	}
private:
	double _threshold;
};*/

#endif

